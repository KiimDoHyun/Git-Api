import React from "react";
import styled from "styled-components";

const SearchRepoListItem = () => {
    return (
        <SearchRepoListItemBlock>SearchRepoListItem</SearchRepoListItemBlock>
    );
};

const SearchRepoListItemBlock = styled.div``;
export default SearchRepoListItem;
