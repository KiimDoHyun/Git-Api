import { atom } from "recoil";

export const rc_drag_showDeleteArea = atom({
    key: "rc_drag_showDeleteArea",
    default: false,
});

export const rc_drag_showSaveArea = atom({
    key: "rc_drag_showSaveArea",
    default: false,
});
